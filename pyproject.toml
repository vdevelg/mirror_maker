[project]
name = "proxy-mirror"
version = "0.1.0"
description = "Mirror maker on Gitverse"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "gitpython>=3.1.58",
    "python-dotenv>=1.2.2",
]
