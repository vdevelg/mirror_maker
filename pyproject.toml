[project]
name = "proxy-mirror"
version = "0.1.0"
description = "Mirror maker on Gitverse"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "gitpython>=3.1.59",
]
